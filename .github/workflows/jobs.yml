name: "Dep jobs Examples"

on: ['push']

jobs:
    job2:
        runs-on: ubuntu-latest
        steps:
            - name: StepA
              run: echo 'Hello'
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: StepB
              run: echo 'Hello'
